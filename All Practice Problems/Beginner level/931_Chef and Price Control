t = int(input())
for i in range(t):
    n,p = map(int,input().split())
    l = list(map(int,input().split()))
    init_sum = sum(l)
    for i in range(len(l)):
        if l[i]>p:
            l[i]=p
    aft_sum = sum(l)
    print( init_sum - aft_sum )
